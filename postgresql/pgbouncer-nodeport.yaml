apiVersion: v1
kind: Service
metadata:
  name: shopixy-pgbouncer-nodeport
  namespace: postgresql

spec:
  type: NodePort

  selector:
    cnpg.io/poolerName: shopixy-pgbouncer

  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30432
