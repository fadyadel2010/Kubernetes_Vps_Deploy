apiVersion: rabbitmq.com/v1beta1
kind: Exchange
metadata:
  name: shopixy-events
  namespace: rabbitmq-lab

spec:
  name: shopixy.events

  type: topic

  durable: true

  autoDelete: false

  vhost: test

  rabbitmqClusterReference:
    name: rabbitmq
