apiVersion: v1
kind: Service
metadata:
  name: mongo-headless
  namespace: mongo
spec:
  clusterIP: None

  selector:
    app: mongo

  ports:
    - name: mongodb
      port: 27017
      targetPort: 27017
