apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP

metadata:
  name: opensearch
  namespace: opensearch

spec:
  entryPoints:
    - opensearch

  routes:
    - match: HostSNI(`*`)
      services:
        - name: shopixy-search
          port: 9200
