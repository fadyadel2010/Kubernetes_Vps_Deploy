apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP

metadata:
  name: minio
  namespace: minio

spec:
  entryPoints:
    - minio

  routes:
    - match: HostSNI(`*`)
      services:
        - name: minio
          port: 9000
