apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP

metadata:
  name: rabbitmq
  namespace: rabbitmq

spec:
  entryPoints:
    - rabbitmq

  routes:
    - match: HostSNI(`*`)
      services:
        - name: rabbitmq
          port: 5672
