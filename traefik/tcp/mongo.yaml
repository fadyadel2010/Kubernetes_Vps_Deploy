apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP

metadata:
  name: mongo
  namespace: mongo

spec:
  entryPoints:
    - mongo

  routes:
    - match: HostSNI(`*`)
      services:
        - name: mongo
          port: 27017
