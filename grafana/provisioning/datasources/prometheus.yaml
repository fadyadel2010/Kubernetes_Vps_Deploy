apiVersion: 1

datasources:

  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    isDefault: true

    url: http://prometheus-kube-prometheus-prometheus.prometheus.svc.cluster.local:9090

    editable: false

    jsonData:
      timeInterval: 15s
