apiVersion: 1

groups:
  - orgId: 1
    name: OpenSearch
    folder: Infrastructure
    interval: 1m

    rules:
      - uid: opensearchdiskwarning

        title: OpenSearch Disk Usage High Warning

        condition: C

        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: ffom74f70o2rkb
            model:
              editorMode: code
              expr: |
                max(
                  (
                    (
                      elasticsearch_filesystem_data_size_bytes
                      -
                      elasticsearch_filesystem_data_available_bytes
                    )
                    /
                    elasticsearch_filesystem_data_size_bytes
                  ) * 100
                )
              instant: true
              intervalMs: 1000
              legendFormat: __auto
              maxDataPoints: 43200
              range: false
              refId: A

          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params:
                      - 85
                    type: gte
                  operator:
                    type: and
                  query:
                    params:
                      - C
                  reducer:
                    params: []
                    type: last
                  type: query

              datasource:
                type: __expr__
                uid: __expr__

              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: C
              type: threshold

        noDataState: Alerting
        execErrState: Alerting

        for: 10m

        annotations:
          summary: OpenSearch disk usage is above 85%
          description: OpenSearch data disk utilization has exceeded 85% for more than 10 minutes. Capacity planning or cleanup may be required.

        labels:
          severity: warning
          stack: opensearch

        isPaused: false
